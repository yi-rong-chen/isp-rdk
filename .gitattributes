# 默认按文本处理并规范化
* text=auto

# 脚本类文件强制 LF（避免上传后在 Linux 执行出错）
*.sh  text eol=lf
*.py  text eol=lf
*.env text eol=lf
*.service text eol=lf
*.yaml text eol=lf
*.yml  text eol=lf

# Windows 专属脚本保留 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
